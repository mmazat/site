<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>  
  <style>
    * {
      margin: 5px;
      box-sizing: border-box;
    }
    body {
      display: flex;
      margin: 1;
      text-align: justify;
      font-family: Arial, sans-serif;
      background-color: #121212;
      color: #e0e0e0;
    }
    .content {
      margin: 20px;
      padding: 20px;
      overflow: auto;
      flex-grow: 1;      
    }
    a {
      color: #bb86fc;
    }
  </style>
</head>
<body>
  <div class="content">
    <h1>{{ title }}</h1>
    <p><em>{{ date |  formatDate('MMMM dd, yyyy') }}</em></p>
    <br></br>
    {{ content | safe }}
  </div>
</body>
</html>