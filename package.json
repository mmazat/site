{
  "name": "hello-world-11ty",
  "version": "1.0.0",
  "description": "A simple Hello World website using 11ty.",
  "main": ".eleventy.js",
  "scripts": {
    "build": "eleventy ",
    "serve": "eleventy --serve",
    "serve_site": "eleventy --serve --pathprefix=site",
    "deploy": "eleventy && gh-pages -d _site"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gh-pages": "^6.2.0"
  }
}
